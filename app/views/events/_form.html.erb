<div class="container">
  <p>Vous organisez un événement sur Plancoët ou alentours ? Vous souhaitez nous le signaler afin de l'annoncer sur le site Internet ? <br>
    Complétez le formulaire ci-dessous le plus précisément possible. Votre proposition sera étudiée par l'équipe en charge du site Internet avant d'être mise en ligne.
  </p>
  <form id="eventForm" class="form-horizontal" data-controller="form" data-action="submit->form#submit">
    <%= simple_form_for event, html: { class: 'form-horizontal' } do |f| %>
      <%= f.input :title, label: 'Titre', required: true %>
      <%= f.input :subtitle , label: 'Introduction', required: true %>
      <%= f.input :category_id, collection: Category.all, as: :select, prompt: "Sélectionnez une catégorie" , label: "Catégorie de l'évènement", required: true %>
      <%= f.input :photo , as: :file, label:'Image principale', required: true %>
      <p style="font-size:12px">N.B. : En nous faisant parvenir une image, vous certifiez en être le propriétaire et vous nous autorisez à l'utiliser sur le site Internet.</p>
      <hr>
      <div class="row">
        <div class="col-md-6">
          <%= f.input :start_date, label: "Date de début",
            as: :string,
            input_html: { data: { controller: "datepicker" } },
            required: true %>
        </div>
        <div class="col-md-6">
          <%= f.input :end_date, label: "Date de fin",
            as: :string,
            input_html: { data: { controller: "datepicker" } }%>
        </div>
      </div>
      <div class="row">
        <div class="col-md-6">
          <%= f.input :hourly_start, label: "Heure de début",
            as: :string,
            input_html: { data: { controller: "timepicker" } }%>
        </div>
        <div class="col-md-6">
          <%= f.input :hourly_end, label: "Heure de fin",
            as: :string,
            input_html: { data: { controller: "timepicker" } } %>
        </div>
      </div>
      <hr>
      <%= f.input :place, label: "Lieu de l'évènement", required: true %>
      <%= f.input :contact, label:'Personne référente', required: true %>
      <%= f.input :phone, label: 'Téléphone'%>
      <%= f.input :email, label: "E-mail", required: true %>
      <%= f.input :website, label: 'Site web' %>
      <hr>
      <%= f.input :content, label: 'Description la plus précise possible', input_html: { rows: 15 }, required: true %>
      <%= f.button :submit, class: "btn btn-primary w-100", value: "Proposer un évènement" %>
    <% end %>
  </form>

  <div id="texteRemplacement" class="d-none text-alert">
    Le formulaire a été soumis avec succès.
  </div>
</div>
