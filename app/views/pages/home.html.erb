<% @general.each do |general| %>
<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.0),rgba(0,0,0,0.1)), url('<%= url_for(general.photo) %>');">
  <div class="container text-center text-white">
    <h1><%= general.title %></h1>
    <p><%= general.subtitle %></p>
   </div>
</div>
<% end %>
 <div class="title-actu-home d-flex align-items-center flex-column justify-content-center">
  <div class="icone-actu-home d-flex justify-content-center align-items-center">
    <i class="fa-solid fa-bullhorn"></i>
  </div>
  <div class="title-under-icone">
    Les dernières actualités
  </div>
</div>
<div class="container mt-4 px-5">
<%# render "shared/listing_cards" %>
</div>
